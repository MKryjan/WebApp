extends layout
block header
      header.header
        #divHeader
          .container#cssmenu
              ul#menuList
                li#start
                  a(href='') Start
                li#day
                  a(href='') Dziennik
                li#diet
                  a(href='') Dieta
                li#browser
                  a(href='http://localhost:3000/products') Wyszukaj produkt
      #unlog
        input( id='unlogButton' type='submit', value='Wyloguj')
block content
    script.
        $(document).ready(function(){
        var userName = $('#user').val();
        $('#greet').append(', '+ userName+' !')
        });
